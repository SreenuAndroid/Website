---
description: Push committed changes to remote repository
---

Push all committed changes to the remote repository.

Steps:

1. Run `git push` to push commits to remote

This command simply pushes your local commits to the remote repository.
